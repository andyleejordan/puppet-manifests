---
:backends:
  - yaml
:yaml:
  :datadir: "%{settings::confdir}/hiera"
:merge_behavior: deeper
:hierarchy:
  - "nodes/%{::hostname}"
  - "os/%{::operatingsystem}"
  - "virtual_%{::is_virtual}"
  - "private/%{::hostname}"
  - private
  - common
