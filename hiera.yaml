---
:backends:
  - yaml
:yaml:
  :datadir: "%{settings::confdir}/hiera"
:hierarchy:
  - "nodes/%{::hostname}"
  - "os/%{::operatingsystem}"
  - "virtual_%{::is_virtual}"
  - "private/%{::hostname}"
  - private
  - common
:logger: console
