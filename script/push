#!/bin/bash
[[ -e script/push ]] || { echo >&2 "Please cd into manifests"; exit 1; }
PUPPETS="krikkit.schwartzmeyer.com:manifests slartibartfast.schwartzmeyer.us:manifests"
parallel rsync -rl --executability --exclude '.git*' --exclude 'ssl' . ::: $PUPPETS
